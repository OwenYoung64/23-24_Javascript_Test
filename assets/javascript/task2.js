/* code for changing the color of the table to red using button1 */
function button2(){
  // make a constant linked to the table id
  const table1 = document.getElementById("table1")
  // make the background of the table red
  table1.style.backgroundColor = "red"
}

/* code for changing the color of the table to green using button2 */
function button3(){
  // same constant as before
  const table1 = document.getElementById("table1")
  // make the background of the table green
  table1.style.backgroundColor = "green"
}

/* code for changing the color of the table to blue using button3 */
function button4(){
  // same constant as before
  const table1 = document.getElementById("table1")
  // make the background of the table blue
  table1.style.backgroundColor = "blue"
}